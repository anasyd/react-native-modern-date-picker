{
  "name": "react-native-modern-date-picker-repo",
  "private": true,
  "scripts": {
    "build": "cd package && npm run build",
    "pack": "cd package && npm pack",
    "publish": "cd package && npm publish --access public",
    "start:example": "cd example && npm install && npm run start"
  }
}
